import os

# Текущая рабочая директория
print(f"Текущая папка: {os.getcwd()}")

# Смена рабочей директории
os.chdir('..')  # На уровень выше
print(f"Теперь в: {os.getcwd()}")
os.chdir('modules')  # Вернуться обратно

# Список файлов и папок
print("\nСодержимое текущей папки:")
for item in os.listdir('.'):
    item_type = '📁' if os.path.isdir(item) else '📄'
    print(f"  {item_type} {item}")

# Рекурсивный обход
for root, dirs, files in os.walk('.'):
    print(f"\nПапка: {root}")
    print(f"  Подпапки: {dirs}")
    print(f"  Файлы: {files}")
    break  # Только первый уровень